services:

  postgres:
    image: postgres:16
    container_name: orion-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: orion_ai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: orion_local_password
    ports:
      - "5434:5432"
    volumes:
      - orion-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d orion_ai"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: orion-redis
    restart: unless-stopped
    ports:
      - "6381:6379"
    volumes:
      - orion_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: orion-backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:Application123@postgres:5432/orion_ai
      REDIS_URL: redis://redis:6379
    ports:
      - "8002:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  orion-postgres-data:
    external: true
  orion_redis_data: